{
  "name": "klendathu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Debug tool that uses Claude and MCP to investigate runtime errors",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean",
    "test": "pnpm build && pnpm -r test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "eslint": "^9.38.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.46.2"
  },
  "engines": {
    "node": ">=20.19",
    "pnpm": ">=9"
  }
}
